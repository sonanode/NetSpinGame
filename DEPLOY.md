# Deploy — NetSpinGame

## GitHub Pages

This folder is self-contained (`assets/`, `sounds/`, `js/`).

Live URL after Pages enable:  
`https://netspingame.github.io/NetSpinGame/`

## Local run

```bash
npx serve . -p 3456
```

Open http://localhost:3456
